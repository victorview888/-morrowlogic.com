# Cloudflare Pages 路径化多语言路由
# /en/、/ja/ 等路径通过 rewrite(200)返回对应页面
# 注意：Cloudflare 先匹配实际文件，未命中才走 _redirects
# 因此 assets/* 不会被错误重写

# 路径化语言：剥离语言前缀，指向"裸"路径
/en/*          /:splat           200
/ja/*          /:splat           200
/zh-TW/*       /:splat           200
/es/*          /:splat           200
/de/*          /:splat           200